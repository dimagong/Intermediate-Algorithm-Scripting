function sumAll(arr) {
  var min, max;
  if(arr[0] <= arr[1]){
    min = arr[0];
    max = arr[1];
  }else{
    min = arr[1];
    max = arr[0];
  }
  
  var sum = 0;
  var i = 0;
  while(min <= max ){
    sum += min;
    min++;
  }
 
  return sum;
}

sumAll([1, 4]);
